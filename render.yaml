services:
  - type: web
    name: nextjs-chatbot
    env: node
    rootDir: nextjs-chatbot
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
